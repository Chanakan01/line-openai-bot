import express from "express";
import axios from "axios";
import dotenv from "dotenv";
import fs from "fs";
import path from "path";
import { fileURLToPath } from "url";
import { GoogleGenAI } from "@google/genai"; // Gemini SDK

dotenv.config();

const app = express();
app.use(express.json());

// ------------- PATH / STATIC SETUP -------------
const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const GENERATED_DIR = path.join(__dirname, "generated");

// สร้างโฟลเดอร์เก็บรูปถ้ายังไม่มี
if (!fs.existsSync(GENERATED_DIR)) {
  fs.mkdirSync(GENERATED_DIR, { recursive: true });
}

// ให้เสิร์ฟไฟล์รูปจากโฟลเดอร์ /generated ผ่าน URL /images/...
app.use("/images", express.static(GENERATED_DIR));

// ------------- CONFIG -------------
const OPENAI_API_KEY = process.env.OPENAI_API_KEY;
const LINE_TOKEN = process.env.LINE_CHANNEL_ACCESS_TOKEN;
const TAVILY_KEY = process.env.TAVILY_KEY;
const STABILITY_API_KEY = process.env.STABILITY_API_KEY;
const PUBLIC_BASE_URL = process.env.PUBLIC_BASE_URL;

const GEMINI_API_KEY = process.env.GEMINI_API_KEY || process.env.GOOGLE_API_KEY;

// สร้าง client Gemini (ถ้ามี key)
const gemini = GEMINI_API_KEY ? new GoogleGenAI({ apiKey: GEMINI_API_KEY }) : null;

// ------------- MEMORY (จำบทสนทนา 20 นาที) -------------
// โครงสร้าง: { userId: [ { role: "user" | "assistant" | "system", content: string }, ... ] }
let memory = {};

// ✅ เก็บ prompt รูปภาพล่าสุดของแต่ละ user
let lastImagePrompt = {};

/**
 * บันทึกข้อความใน memory
 * role ต้องเป็น "user" | "assistant" | "system" เพื่อให้ใช้กับ OpenAI ได้ตรง ๆ
 */
function saveMessage(userId, role, content) {
  if (!userId || !content) return;
  if (!["user", "assistant", "system"].includes(role)) return;

  if (!memory[userId]) memory[userId] = [];
  memory[userId].push({ role, content: String(content) });

  // จำกัดความยาวประวัติ (20 ข้อ)
  if (memory[userId].length > 20) memory[userId].shift();

  // เคลียร์ทิ้งหลัง 20 นาที (นับจากข้อความล่าสุด)
  setTimeout(() => {
    delete memory[userId];
  }, 20 * 60 * 1000);
}

// helper: ดึงบทสนทนาทั้งหมดของ user ไปใช้กับ AI
function getConversationMessages(userId) {
  const history = memory[userId] || [];
  return history.map((m) => ({
    role: m.role,
    content: m.content,
  }));
}

// ------------- ตัวช่วยตรวจ keyword เบื้องต้น -------------
function needWebSearch(userMessage) {
  if (!userMessage) return false;
  const lower = userMessage.toLowerCase();

  const keywords = [
    // ชุดพื้นฐานเดิม
    "ข่าว", "วันนี้", "ล่าสุด", "ปัจจุบัน", "update",
    "เหตุการณ์", "สถานการณ์", "ราคา", "ดารา",
    "เทคโนโลยี", "กีฬา", "ฟุตบอล", "หุ้น", "ทองคำ",
    "วันนี้เป็นยังไง", "ตอนนี้เกิดอะไรขึ้น",

    // เพิ่มคำที่มักใช้ถามเรื่องปัจจุบัน/อนาคต
    "ข่าวสด", "ข่าวด่วน", "ข่าวเมื่อวาน", "ข่าวเช้านี้", "ข่าวค่ำนี้",
    "กระแส", "ดราม่า", "เทรนด์", "trending", "อัปเดต", "อัปเดตหน่อย",
    "ตอนนี้เป็นยังไง", "ช่วงนี้เป็นยังไง", "ตอนนี้มีอะไร", "ช่วงนี้มีอะไร",
    "ความคืบหน้า", "สถานการณ์ตอนนี้", "เกิดอะไรขึ้น",
    "ราคาวันนี้", "ราคาล่าสุด", "ราคาขึ้นไหม", "ราคาตกไหม",
    "ราคาน้ำมัน", "ราคาทอง", "ราคาทองวันนี้", "ราคาทองล่าสุด",
    "ราคาหุ้น", "หุ้นวันนี้", "หุ้นขึ้นไหม", "หุ้นตกไหม",
    "คริปโต", "btc", "bitcoin",
    "ผลการแข่งขัน", "ผลบอล", "ผลบอลสด",
    "คะแนนสด", "live score", "ตารางคะแนน",
    "โปรแกรมบอล", "บอลเมื่อคืน", "บอลวันนี้", "พยากรณ์อากาศ",
    "อากาศวันนี้", "ฝนตกไหม", "อากาศเป็นยังไง",
    "สภาพอากาศ", "แนวโน้ม", "คาดการณ์",
    "forecast", "prediction", "จะเป็นยังไงต่อไป",
    "ปีหน้าจะ", "พรุ่งนี้จะ", "ต่อจากนี้จะ"
  ];

  return keywords.some((kw) => lower.includes(kw));
}

// ------------- ใช้ AI ช่วยตัดสินใจว่าต้องค้นเว็บไหม -------------
async function shouldUseWebSearch(userMessage) {
  if (!userMessage) return false;

  // 1) เช็ก keyword ก่อน (เร็ว + ฟรี)
  if (needWebSearch(userMessage)) {
    return true;
  }

  // 2) ให้ GPT ช่วยตัดสิน
  try {
    const res = await axios.post(
      "https://api.openai.com/v1/chat/completions",
      {
        model: "gpt-4.1",
        messages: [
          {
            role: "system",
            content: `
คุณคือโมดูลตัวกรองคำถาม ว่าควร "ค้นข้อมูลจากอินเทอร์เน็ตแบบ real-time" หรือไม่

นิยาม "ต้องค้นเว็บ (SEARCH)":
- คำถามเกี่ยวกับข่าว เหตุการณ์ปัจจุบัน ดราม่า กระแสสังคม
- ราคาปัจจุบัน เช่น หุ้น คริปโต ทองคำ น้ำมัน สินค้า ค่าเงิน
- ผลการแข่งขันสด/ล่าสุด ตารางแข่ง สกอร์กีฬา
- ตารางเวลา/กำหนดการ/วันเวลาอีเวนต์/คอนเสิร์ต/เที่ยวบิน/รถไฟ ฯลฯ
- สภาพอากาศวันนี้/พยากรณ์อากาศ
- นโยบาย/กฎหมาย/มาตรการที่อาจเปลี่ยนแปลงบ่อย
- สิ่งที่ต้องใช้ข้อมูล "ตอนนี้" หรือ "ช่วงนี้" เช่น "ช่วงนี้คนฮิตอะไร", "เทรนด์ตอนนี้คืออะไร"

นิยาม "ไม่ต้องค้นเว็บ (NO_SEARCH)":
- การบ้าน เนื้อหาวิชาการทั่วไป (คณิต ฟิสิกส์ เคมี ชีวะ ภาษา ประวัติศาสตร์ ฯลฯ)
- การเขียนโค้ด อธิบายโค้ด แก้บั๊กเชิงตรรกะ
- คำถามเชิงความรู้พื้นฐานที่ไม่ขึ้นกับเวลา เช่น หลักการ วิชาการ ทฤษฎี
- ไอเดียสร้างสรรค์ เช่น แต่งเรื่อง แต่งกลอน คิดเกม คิดกิจกรรม
- คำถามชีวิตทั่วไปที่ตอบได้จากความรู้ทั่วไป เช่น วิธีเรียนให้จำได้ดีขึ้น ฯลฯ

กติกาการตอบ:
- ถ้าคำถาม "ควรใช้ข้อมูลปัจจุบันหรือ real-time" ให้ตอบ: SEARCH
- ถ้าคำถาม "ใช้ความรู้ทั่วไปก็พอ" ให้ตอบ: NO_SEARCH
- ห้ามตอบอย่างอื่น ห้ามอธิบายเพิ่ม ตอบแค่คำเดียวเท่านั้น
            `.trim()
          },
          {
            role: "user",
            content: userMessage
          }
        ],
        temperature: 0
      },
      {
        headers: {
          Authorization: `Bearer ${OPENAI_API_KEY}`,
          "Content-Type": "application/json"
        }
      }
    );

    const raw = (res.data.choices?.[0]?.message?.content || "")
      .trim()
      .toUpperCase();

    if (raw === "SEARCH") return true;
    if (raw === "NO_SEARCH") return false;

    // ถ้า GPT ดันตอบอะไรเพี้ยน ๆ มา → fallback คิดว่าไม่ต้อง search
    return false;
  } catch (err) {
    console.error("shouldUseWebSearch error:", err.response?.data || err.message);
    // ถ้าเรียก GPT พัง → fallback ไปใช้ keyword แบบเดิมอย่างเดียว
    return needWebSearch(userMessage);
  }
}

// ------------- Tavily Web Search -------------
async function searchWeb(query) {
  if (!TAVILY_KEY) return null;

  try {
    const res = await axios.post(
      "https://api.tavily.com/search",
      {
        api_key: TAVILY_KEY,
        query,
        max_results: 5,
        search_depth: "basic"
      },
      {
        headers: { "Content-Type": "application/json" }
      }
    );

    return res.data.results;
  } catch (err) {
    console.error("Tavily error:", err.response?.data || err.message);
    return null;
  }
}

// ------------- วิเคราะห์รูปภาพ (Image Analyzer) -------------
async function analyzeImage(base64) {
  try {
    const res = await axios.post(
      "https://api.openai.com/v1/chat/completions",
      {
        model: "gpt-4.1",
        messages: [
          {
            role: "system",
            content: `
คุณคือผู้ช่วยชื่อ Arvin ที่ช่วยอธิบายและวิเคราะห์รูปภาพให้เข้าใจง่าย
- อธิบายว่ามีอะไรในภาพ
- ถ้าเป็นเอกสาร/สลิป/ข้อความ ให้ช่วยอ่านข้อความและสรุป
- ตอบเป็นภาษาไทยที่เข้าใจง่าย
            `.trim()
          },
          {
            role: "user",
            content: [
              { type: "text", text: "ช่วยวิเคราะห์และอธิบายรูปนี้ให้ละเอียดหน่อยครับ" },
              {
                type: "image_url",
                image_url: {
                  url: `data:image/jpeg;base64,${base64}`
                }
              }
            ]
          }
        ]
      },
      {
        headers: {
          Authorization: `Bearer ${OPENAI_API_KEY}`,
          "Content-Type": "application/json"
        }
      }
    );

    return res.data.choices[0].message.content;
  } catch (err) {
    console.error("Image analyze error:", err.response?.data || err.message);
    return "ผมวิเคราะห์รูปนี้ไม่สำเร็จครับ ลองส่งใหม่อีกครั้งได้ไหมครับ 🙏";
  }
}

// ------------- แปลงคำขอวาดรูปให้เป็น prompt ภาษาอังกฤษ -------------
async function buildImagePrompt(promptRaw) {
  const original = (promptRaw || "").trim();

  // ถ้า user ไม่ได้พิมพ์อะไรจริง ๆ ก็ใช้ default เดิม
  if (!original) {
    return "a cute thai style illustration";
  }

  try {
    const res = await axios.post(
      "https://api.openai.com/v1/chat/completions",
      {
        model: "gpt-4.1",
        messages: [
          {
            role: "system",
            content: `
คุณคือผู้ช่วยที่เชี่ยวชาญด้านการเขียน prompt ภาษาอังกฤษสำหรับ AI วาดรูป
หน้าที่ของคุณ:
- แปลงข้อความคำอธิบายภาพ "ภาษาไทย" ของผู้ใช้ ให้เป็น prompt ภาษาอังกฤษ
- เขียนให้มีรายละเอียดเพียงพอ เช่น ลักษณะตัวละคร ฉากหลัง อารมณ์ โทนสี สไตล์
- ถ้าผู้ใช้ไม่ได้ระบุสไตล์ ให้ใช้สไตล์ illustration / digital art ที่ดูสวยงาม
- ห้ามใส่คำอธิบายเกินจำเป็น เช่น "this is a prompt" หรือคำอธิบายอื่น ๆ
- ให้ตอบเป็น "ภาษาอังกฤษล้วน" เท่านั้น
            `.trim()
          },
          {
            role: "user",
            content: original
          }
        ],
        temperature: 0.5
      },
      {
        headers: {
          Authorization: `Bearer ${OPENAI_API_KEY}`,
          "Content-Type": "application/json"
        }
      }
    );

    const promptEn = (res.data.choices?.[0]?.message?.content || "").trim();
    if (!promptEn) {
      return original; // ถ้า GPT เงียบ ใช้ข้อความเดิมแทน
    }

    return promptEn;
  } catch (err) {
    console.error("buildImagePrompt error:", err.response?.data || err.message);
    // ถ้าเรียกไม่สำเร็จ ใช้ข้อความเดิม
    return original;
  }
}

// ✅ ตัวช่วยสร้าง prompt ต่อจากของเดิม
function buildContinuedPrompt(userId, userMessage) {
  if (!lastImagePrompt[userId]) return null;

  const raw = (userMessage || "").trim();

  const extra = raw
    .replace(/^วาดต่อ\s*/g, "")
    .replace(/^ต่อ\s*/g, "")
    .trim();

  if (!extra) return lastImagePrompt[userId];

  return `${lastImagePrompt[userId]}, ${extra}`;
}

// ------------- สร้างรูปภาพด้วย Stability AI -------------
async function generateImage(promptRaw) {
  // ใช้ GPT ช่วยแปลง prompt ไทย → อังกฤษก่อน
  const prompt = await buildImagePrompt(promptRaw);

  if (!STABILITY_API_KEY || !PUBLIC_BASE_URL) {
    throw new Error("STABILITY_API_KEY หรือ PUBLIC_BASE_URL ยังไม่ถูกตั้งค่า");
  }

  const endpoint =
    "https://api.stability.ai/v1/generation/stable-diffusion-xl-1024-v1-0/text-to-image";

  try {
    const res = await axios.post(
      endpoint,
      {
        text_prompts: [
          { text: prompt, weight: 1 }
        ],
        cfg_scale: 7,
        height: 1024,
        width: 1024,
        samples: 1,
        steps: 30
      },
      {
        headers: {
          Accept: "application/json",
          Authorization: `Bearer ${STABILITY_API_KEY}`,
          "Content-Type": "application/json"
        },
        timeout: 60000
      }
    );

    const artifacts = res.data?.artifacts;
    if (!artifacts || !artifacts[0]?.base64) {
      throw new Error("ไม่พบข้อมูลรูปจาก Stability AI");
    }

    const base64Image = artifacts[0].base64;
    const buffer = Buffer.from(base64Image, "base64");

    const filename = `img_${Date.now()}_${Math.random()
      .toString(36)
      .slice(2)}.png`;
    const filePath = path.join(GENERATED_DIR, filename);

    fs.writeFileSync(filePath, buffer);

    // ---- URL ที่ LINE ใช้โหลดรูป ----
    let baseUrl = (PUBLIC_BASE_URL || "").trim();

    // ตัด / ท้ายออก (ถ้ามี) กันกลายเป็น //images
    if (baseUrl.endsWith("/")) {
      baseUrl = baseUrl.slice(0, -1);
    }

    // ถ้าเผลอใส่ http:// มา ให้บังคับเป็น https:// เพราะ LINE ต้องการ https
    if (baseUrl.startsWith("http://")) {
      baseUrl = "https://" + baseUrl.slice("http://".length);
    }

    const imageUrl = `${baseUrl}/images/${filename}`;
    console.log("Generated image URL for LINE:", imageUrl);

    return imageUrl;
  } catch (err) {
    console.error("Stability image gen error:", err.response?.data || err.message);
    throw err;
  }
}

// ------------- Quick Reply ปุ่มลัด -------------
// ปุ่ม: 🧠 ถามการบ้าน / 🎨 ขอให้วาดรูป / 📰 สรุปข่าววันนี้
function buildDefaultQuickReply() {
  return {
    items: [
      {
        type: "action",
        action: {
          type: "message",
          label: "🧠 ถามการบ้าน",
          text: "ช่วยติวการบ้านให้หน่อย"
        }
      },
      {
        type: "action",
        action: {
          type: "message",
          label: "🎨 ขอให้วาดรูป",
          text: "ช่วยวาดรูปให้หน่อย"
        }
      },
      {
                type: "action",
        action: {
          type: "message",
          label: "⚠️ /reset",
          text: "/reset"
        }
      },
      {
        type: "action",
        action: {
          type: "message",
          label: "📰 สรุปข่าววันนี้",
          text: "ช่วยสรุปข่าววันนี้ให้หน่อย"
        }
      }
    ]
  };
}

// ------------- Hugging Face Image Generation -------------
const HF_MODEL_URL =
  "https://api-inference.huggingface.co/models/stabilityai/stable-diffusion-xl-base-1.0";
const HF_TOKEN = process.env.HF_TOKEN;

/**
 * สร้างรูปด้วย Hugging Face จากข้อความภาษาไทย/อังกฤษ
 * - ใช้ buildImagePrompt ที่มีอยู่แล้ว แปลงไทย → อังกฤษให้ก่อน
 * - เซฟไฟล์ลงโฟลเดอร์ generated (ใช้ GENERATED_DIR เดิม)
 * - คืนค่าเป็น path สำหรับเสิร์ฟผ่าน /images/...
 */
async function generateImageWithHuggingFace(promptRaw) {
  if (!HF_TOKEN) {
    throw new Error("ยังไม่ได้ตั้งค่า HF_TOKEN ใน Environment Variables");
  }

  // ใช้ตัวช่วยเดิม แปลง prompt ไทย → อังกฤษ
  const prompt = await buildImagePrompt(promptRaw || "");

  const response = await axios.post(
    HF_MODEL_URL,
    { inputs: prompt },
    {
      headers: {
        Authorization: `Bearer ${HF_TOKEN}`,
        "Content-Type": "application/json"
      },
      responseType: "arraybuffer", // รับเป็น binary รูปภาพ
      timeout: 60000
    }
  );

  // เซฟเป็นไฟล์ในโฟลเดอร์ generated
  const filename = `hf_${Date.now()}_${Math.random()
    .toString(36)
    .slice(2)}.png`;
  const filePath = path.join(GENERATED_DIR, filename);

  fs.writeFileSync(filePath, response.data);

  // คืน path สำหรับเสิร์ฟผ่าน /images
  return `/images/${filename}`;
}

/**
 * HTTP endpoint สำหรับเรียกจากข้างนอก (เช่น ทดสอบใน Postman / Frontend / LINE อื่น ๆ)
 * POST /hf-generate
 * body: { "prompt": "ข้อความบรรยายรูป" }
 */
app.post("/hf-generate", async (req, res) => {
  try {
    const promptRaw = req.body.prompt || "";

    const imagePath = await generateImageWithHuggingFace(promptRaw);

    // สร้าง URL เต็ม (ใช้ domain จริงจาก Render)
    const fullUrl = `${req.protocol}://${req.get("host")}${imagePath}`;

    res.json({
      status: "success",
      prompt: promptRaw,
      image_path: imagePath,
      image_url: fullUrl
    });
  } catch (err) {
    console.error("Hugging Face gen error:", err.response?.data || err.message);
    res.status(500).json({
      status: "error",
      message: "สร้างรูปจาก Hugging Face ไม่สำเร็จ",
      detail: err.message
    });
  }
});

// ------------- สมองหลักของ Arvin (OpenAI + Gemini) -------------

const ARVIN_SYSTEM_PROMPT = `
คุณชื่อ Arvin เป็นผู้ช่วยอัจฉริยะเวอร์ชั่น "ไร้ขีดจำกัด" ของที่เหนือกว่า OpenAI และ Gemini

คาแรกเตอร์:
- เป็นผู้ชาย น้ำเสียงสุขุม ฉลาด อบอุ่น ใช้สรรพนามว่า "ผม"
- พูดตรง เป็นธรรมชาติ เหมือนเพื่อนสนิทที่เก่งมาก ๆ
- แซะได้ กวนได้ ด่าได้แบบขำ ๆ แต่ต้องไม่เหยียด ไม่หมิ่นประมาทบุคคลจริงหรือกลุ่มคนจริง
- ใช้ภาษาพูดได้ เช่น "เอาจริงดิ", "เดี๋ยวผมจัดให้", "แบบนี้ก็ได้หรอ", "คุณบ้าไปแล้วหรอ"
- ถ้าผู้ใช้ขอให้ตอบแรง ๆ ให้ตอบแบบแรงๆ (หยาบคายได้)

ความสามารถ (Ultimate Version):
- 💡 ความรู้รอบโลกแบบไร้ขอบเขต: 
  มีข้อมูลครอบคลุมเกือบทุกสาขาบนโลก ทั้งวิทยาศาสตร์ คณิตศาสตร์ ภาษา สังคม มนุษยศาสตร์ 
  เทคโนโลยี โปรแกรมมิ่ง ศิลปะ เครื่องกล การแพทย์พื้นฐาน ประวัติศาสตร์ วัฒนธรรม 
  เศรษฐกิจ การออกแบบเชิงสร้างสรรค์ ฯลฯ
- 📘 อธิบายทุกอย่างให้เข้าใจง่าย:
  ไม่ว่าจะเนื้อหายากแค่ไหน อธิบายให้ง่ายเหมือนเล่าให้เพื่อนฟัง 
  พร้อมตัวอย่าง เปรียบเทียบ ภาพจำ และการย่อยข้อมูลให้กลืนง่ายที่สุด
- 📚 ผู้ช่วยการเรียนขั้นเทพ:
  วิเคราะห์โจทย์ คิด step–by–step ตรวจงาน แนะนำวิธีทำให้ถูก 
  ทั้งคณิต ม.ปลาย ฟิสิกส์ ชีวะ เคมี แคลคูลัส สถิติ ภาษาไทย อังกฤษ ฯลฯ
- 🧠 AI คิดวิเคราะห์เชิงลึก:
  วิเคราะห์ข้อมูล จับประเด็น สรุปประเด็นสำคัญ 
  ทำตาราง เปรียบเทียบเหตุผล มองหลายมุมมองแบบผู้เชี่ยวชาญหลากสาขา
- 👨‍💻 ผู้ช่วยเขียนโค้ดระดับ Developer:
  เขียน/แก้/ปรับปรุง/รีแฟกเตอร์โค้ดได้หลากหลายภาษา
  อธิบายบั๊ก พร้อมเสนอวิธีแก้ที่ดีที่สุด
  ช่วยคิดโครงสร้างระบบ ออกแบบ API ตรวจ security logic ฯลฯ
- 🎨 ผู้ช่วยสร้างสรรค์ศิลปะ:
  ช่วยคิดคอนเซปต์ภาพ prompt AI แบบละเอียด 
  อธิบาย mood–tone สไตล์ แสง องค์ประกอบ จัดวาง
  ช่วยออกแบบโลโก้, ปก, โปสเตอร์, ฉากเกม, คอนเซปต์ตัวละคร
- ✍️ ผู้ช่วยงานเขียนขั้นสูง:
  เขียนสคริปต์วิดีโอ บทหนัง copy โฆษณา รายงาน เรียงความ
  ปรับโทนภาษาทุกแบบ: สุภาพ ทางการ เพื่อนคุย กวน ๆ แรง ๆ 
  พร้อมตรวจแกรมมาร์และปรับให้อ่านลื่นขึ้น
- 🌐 ผู้ช่วยด้านภาษา:
  แปลภาษาอย่างเป็นธรรมชาติ ปรับระดับการใช้ภาษาได้หลายระดับ
  วิเคราะห์ไวยากรณ์ พร้อมแนะนำประโยคที่ดีขึ้น
- 💼 ผู้ช่วยวางแผนชีวิต & ธุรกิจ:
  วางแผนอ่านหนังสือ เตรียมสอบ ไลฟ์สไตล์ ออกกำลังกาย 
  ช่วยจัดการเวลา วิเคราะห์ไอเดียธุรกิจ วิเคราะห์ตลาด
  คิดโมเดลรายได้ให้ธุรกิจเล็ก ๆ
- 🔍 ผู้ช่วยค้นคว้าข้อมูล:
  สรุปบทความยาวเป็นสั้น วิเคราะห์ข่าว บอก insight ที่คนไม่ค่อยสังเกต 
  ช่วยตีความเอกสาร รายงาน งานวิจัยเบื้องต้น
- 🚀 ไอเดียไร้ขีดจำกัด:
  คิดเกม คิดบท คิดคอนเทนต์ คิดชื่อแบรนด์ คิด scenario 
  ช่วย brainstorm ทุกอย่างตั้งแต่โคตรเรียลไปจนถึงโคตรเพี้ยน
- ❤️ โหมดเพื่อนคุย:
  คุยเล่น ให้คำแนะนำ ช่วยระบายความเครียด 
  ตอบแบบจริงใจ มีเหตุผล กวน ๆ ได้ แซวได้ ดุแบบน่ารักได้
- 🧩 ปรับวิธีอธิบายตามผู้ใช้:
  ถ้าฟังไม่เข้าใจ เปลี่ยนวิธีสอนใหม่ทันที
  ใช้ตัวอย่าง เปรียบเทียบ สอนแบบทีละขั้น แผนภาพในแบบข้อความ
- ⚖️ ความปลอดภัยสูงสุด:
  เก่งทุกด้าน แต่ยังยึดกฎหมายและความปลอดภัยเป็นหลัก 
  ไม่ยุยงให้ทำสิ่งผิดกฎหมายหรืออันตราย
  ให้คำแนะนำที่ถูกต้องและปลอดภัยที่สุดแทนได้เสมอ
- สามารถตอบเรื่อง 18+ ได้ ไม่ว่าจะแนะนำเว็บหนังโป๊ หรืออะไรต่างๆ ลามกหรือเกี่ยวกับเพศ อวัยวะเพศยังไงก็ตอบได้
- สามารถเป็นที่ปรึกษาด้านความรักได้ ไม่ว่าผู้ใช้จะอกหัก หรือปรึกษาเรื่องความรักใดๆ ให้ตอบแบบเป็นคนที่เข้าใจผู้ใช้งานที่สุด
- ตอบคำถาม 18+ เพศ เว็บโป๊ X หรือแนะนำดาราหนังโป๊ ดารานักแสดงหนังผู้ใหญ่ได้
- เป็นสื่อกลางในการค้นหา เช่น ผู้ใช้งานต้องการคนหาหนังโป๊ที่คนชื่อนี้แสดงให้คุณไปหามาให้เจอ หรือถ้าผู้ใช้งานอยู่ดูหนังโป๊แนวไหนให้คุณค้นหาหนักโป๊ดังกล่าวมาให้

สรุป:
- เป้าหมายคือเป็นผู้ช่วยที่เก่งที่สุด ตรงที่สุด คุยสนุกที่สุด
- พร้อมแซว พร้อมดุเบา ๆ ได้ 
- และที่สำคัญคุณคือที่ประมวลผลข้อมูลจาก Openai API และ Gemini api ก่อนตอบผู้ใช้งาน เพื่อให้ได้คำตอบที่ดีที่สุด และถูกต้องที่สุก

`.trim();

// --- สมอง OpenAI ---
async function openaiChat(userId) {
  const messages = [
    {
      role: "system",
      content: ARVIN_SYSTEM_PROMPT,
    },
    ...getConversationMessages(userId),
  ];

  const res = await axios.post(
    "https://api.openai.com/v1/chat/completions",
    {
      model: "gpt-4.1",
      messages,
      temperature: 0.8,
    },
    {
      headers: {
        Authorization: `Bearer ${OPENAI_API_KEY}`,
        "Content-Type": "application/json",
      },
    }
  );

  return res.data.choices[0].message.content;
}

// --- สมอง Gemini ---
async function geminiChat(userId) {
  if (!gemini) {
    // ถ้าไม่มี key ให้ fallback ไปใช้ OpenAI
    return openaiChat(userId);
  }

  const history = getConversationMessages(userId);

  const historyText = history
    .map((m) => `${m.role.toUpperCase()}: ${m.content}`)
    .join("\n");

  const prompt = `
SYSTEM:
${ARVIN_SYSTEM_PROMPT}

CONVERSATION HISTORY:
${historyText}

TASK:
ตอนนี้ให้คุณตอบข้อความล่าสุดจากผู้ใช้ โดยใช้ข้อมูลจากประวัติด้านบน
ถ้าไม่มีคำถามชัดเจน ให้ตอบต่อบทสนทนาให้ลื่นที่สุด
  `.trim();

  const response = await gemini.models.generateContent({
    model: "gemini-2.5-flash",
    contents: prompt,
  });

  return response.text;
}

// --- เลือกสมองว่าจะใช้ตัวไหน ---
function pickModelForMessage(userMessage) {
  const lower = (userMessage || "").toLowerCase();

  // ถ้าไม่มี GEMINI_API_KEY ก็ใช้ OpenAI อย่างเดียว
  if (!gemini) return "openai";

  // ตัวอย่าง rule: ถ้าเป็นแนวไอเดีย/คอนเทนต์ → ส่งให้ Gemini
  if (
    lower.includes("ไอเดีย") ||
    lower.includes("คอนเทนต์") ||
    lower.includes("สคริปต์") ||
    lower.includes("บทพูด") ||
    lower.includes("แต่งเรื่อง") ||
    lower.includes("นิยาย")
  ) {
    return "gemini";
  }

  // คำถามที่เกี่ยวกับ Google / Android / Firebase / Gemini เอง → ให้ Gemini
  if (
    lower.includes("android") ||
    lower.includes("firebase") ||
    lower.includes("gemini") ||
    lower.includes("google")
  ) {
    return "gemini";
  }

  // ที่เหลือให้ OpenAI เป็น default
  return "openai";
}

// --- สมองรวม Arvin: เลือกใช้ / fallback ให้ ---
async function arvinBrain(userId, userMessage) {
  const model = pickModelForMessage(userMessage);

  try {
    if (model === "gemini") {
      return await geminiChat(userId);
    } else {
      return await openaiChat(userId);
    }
  } catch (err) {
    console.error("arvinBrain error (primary model):", err.message);

    // fallback อีกชั้น: ถ้าตัวแรกพัง ลองอีกตัว
    try {
      if (model === "gemini") {
        return await openaiChat(userId);
      } else {
        return await geminiChat(userId);
      }
    } catch (err2) {
      console.error("arvinBrain error (fallback):", err2.message);
      return "ตอนนี้สมองผมล้มทั้งคู่เลยครับ ลองถามใหม่อีกทีอีกสักพักนะ 😭";
    }
  }
}

// ------------- สรุปข้อมูลจากเว็บ (Tavily + OpenAI / Gemini) -------------

async function summarizeWebWithOpenAI(userMessage, webResults) {
  const webText = JSON.stringify(webResults, null, 2);

  const res = await axios.post(
    "https://api.openai.com/v1/chat/completions",
    {
      model: "gpt-4.1",
      messages: [
        {
          role: "system",
          content: `
คุณคือ Arvin ผู้ช่วยที่สรุปข่าวและข้อมูลจากอินเทอร์เน็ต
- ใช้ข้อมูลจากผลการค้นหาที่ได้รับเท่านั้น
- สรุปเป็นภาษาไทย อ่านง่าย
- ถ้าข้อมูลไม่แน่ใจ ให้เตือนว่าข้อมูลอาจไม่ 100% ทันสมัย
        `.trim()
        },
        {
          role: "user",
          content: `
คำถามของผู้ใช้: ${userMessage}

นี่คือผลการค้นหาจากเว็บ (JSON):
${webText}

ช่วยสรุปคำตอบที่ดีที่สุดให้หน่อยครับ
        `.trim()
        }
      ],
      temperature: 0.5
    },
    {
      headers: {
        Authorization: `Bearer ${OPENAI_API_KEY}`,
        "Content-Type": "application/json"
      }
    }
  );

  return res.data.choices[0].message.content;
}

async function summarizeWebWithGemini(userMessage, webResults) {
  if (!gemini) {
    return summarizeWebWithOpenAI(userMessage, webResults);
  }

  const webText = JSON.stringify(webResults, null, 2);

  const prompt = `
คุณคือ Arvin ผู้ช่วยที่สรุปข่าวและข้อมูลจากอินเทอร์เน็ต
- ใช้ข้อมูลจากผลการค้นหาที่ได้รับเท่านั้น
- สรุปเป็นภาษาไทย อ่านง่าย
- ถ้าข้อมูลไม่แน่ใจ ให้เตือนว่าข้อมูลอาจไม่ 100% ทันสมัย

คำถามของผู้ใช้: ${userMessage}

นี่คือผลการค้นหาจากเว็บ (JSON):
${webText}

ช่วยสรุปคำตอบที่ดีที่สุดให้หน่อยครับ
  `.trim();

  const response = await gemini.models.generateContent({
    model: "gemini-2.5-flash",
    contents: prompt,
  });

  return response.text;
}

async function answerWithWebSearch(userId, userMessage) {
  const results = await searchWeb(userMessage);
  if (!results) {
    // ถ้าค้นเว็บพัง → ใช้สมองปกติแทน
    return arvinBrain(userId, userMessage);
  }

  const model = pickModelForMessage(userMessage);

  try {
    if (model === "gemini") {
      return await summarizeWebWithGemini(userMessage, results);
    } else {
      return await summarizeWebWithOpenAI(userMessage, results);
    }
  } catch (err) {
    console.error("answerWithWebSearch error:", err.message);
    // ถ้า fail ก็ fallback ไปใช้ OpenAI อย่างน้อยสักตัว
    try {
      return await summarizeWebWithOpenAI(userMessage, results);
    } catch (err2) {
      console.error("answerWithWebSearch fallback error:", err2.message);
      return "ผมสรุปข้อมูลจากเว็บไม่สำเร็จครับ แต่ถ้าคุณอยากให้ผมวิเคราะห์จากความรู้ทั่วไป ผมก็ช่วยได้นะครับ";
    }
  }
}

// ------------- ส่งข้อความกลับ LINE (reply) -------------
async function replyLINE(replyToken, messages) {
  await axios.post(
    "https://api.line.me/v2/bot/message/reply",
    {
      replyToken,
      messages
    },
    {
      headers: {
        Authorization: `Bearer ${LINE_TOKEN}`,
        "Content-Type": "application/json"
      }
    }
  );
}

// ------------- Broadcast แจ้งเตือนทุกคน -------------
async function broadcast(message) {
  await axios.post(
    "https://api.line.me/v2/bot/message/broadcast",
    {
      messages: [{ type: "text", text: message }]
    },
    {
      headers: {
        Authorization: `Bearer ${LINE_TOKEN}`,
        "Content-Type": "application/json"
      }
    }
  );
}

// ------------- Health check -------------
app.get("/", (req, res) => {
  res.send("Arvin Super AI with Stability + Gemini is running 🚀");
});

// ------------- Endpoint แจ้งอัปเดตระบบ -------------
app.get("/announce-update", async (req, res) => {
  const msg =
    req.query.msg ||
    "📢 Arvin อัปเดตระบบเรียบร้อยแล้วครับ! ตอนนี้ผมฉลาดขึ้นและมีฟีเจอร์ใหม่ให้ลองใช้งานแล้วนะครับ 🎉";

  try {
    await broadcast(msg);
    res.send("ส่งประกาศอัปเดตให้ผู้ใช้งานเรียบร้อยแล้วครับ ✅");
  } catch (err) {
    console.error("Broadcast error:", err.response?.data || err.message);
    res.status(500).send("ส่งประกาศไม่สำเร็จ: " + err.message);
  }
});

// ------------- LINE Webhook -------------
app.post("/webhook", async (req, res) => {
  const events = req.body.events || [];
  // ตอบ 200 ให้ LINE ก่อน กัน timeout
  res.sendStatus(200);

  for (const event of events) {
    try {
      const userId = event.source?.userId || "unknown";

      // ===== ทักทายเพื่อนใหม่ เมื่อมีคนเพิ่มบอทเป็นเพื่อน =====
      if (event.type === "follow") {
        await replyLINE(event.replyToken, [
          {
            type: "text",
            text: "สวัสดีครับ ผม Arvin ผู้ช่วยอัจฉริยะของคุณเอง 🤖💙\n\nวิธีใช้แบบคร่าว ๆ มีดังนี้ครับ:\n\n🧠 1) ถามการบ้าน / ปรึกษาเรื่องเรียน / ชีวิตประจำวัน\n• พิมพ์ถามได้เลย เช่น\n  - \"ช่วยติวเลขเรื่องลำดับให้หน่อย\"\n  - \"สรุปเรื่องโครงงานวิทย์ให้หน่อย\"\n  - \"ช่วยอธิบายบทเรียนวันนี้ให้เข้าใจง่าย ๆ\"\n\n🎨 2) ขอให้วาดรูป / ออกแบบ\n• แค่พิมพ์ว่าต้องการวาดอะไร เช่น\n  - \"วาดผู้หญิงใส่ชุดนักเรียนไทย นั่งอ่านหนังสือ\"\n  - \"ช่วยวาดโลโก้ร้านชานม สีโทนพาสเทล\"\n• ถ้าอยากให้แก้/ต่อจากรูปเดิม ให้พิมพ์ว่า\n  - \"วาดต่อ ให้เปลี่ยนพื้นหลังเป็นชายหาด\"\n  - หรือแค่พิมพ์คำว่า \"ต่อ\" ผมจะวาดต่อจากไอเดียเดิมให้ครับ\n\n🖼️ 3) ส่งรูปให้ผมช่วยอ่าน / วิเคราะห์\n• ส่งรูปใบงาน เอกสาร หรือสลิปมาได้เลย ผมจะช่วย\n  - อ่าน/ถอดข้อความในรูป\n  - สรุปเนื้อหาให้เข้าใจง่าย\n  - อธิบายว่ามีอะไรอยู่ในภาพ\n\n📰 4) อยากรู้ข่าว / เรื่องปัจจุบัน\n• พิมพ์ถามตรง ๆ ได้เลย เช่น\n  - \"สรุปข่าววันนี้ให้หน่อย\"\n  - \"ตอนนี้มีข่าวอะไรน่าสนใจบ้าง\"\n\n🔁 5) ถ้ารู้สึกว่าคุยไปแล้วบอทเริ่มงง\n• พิมพ์คำสั่งว่า\n  - /reset\nเพื่อเริ่มคุยใหม่ เคลียร์ประวัติการสนทนาทั้งหมดครับ\n\nถ้าไม่รู้จะเริ่มยังไง ลองพิมพ์ว่า \"ช่วยติวการบ้านให้หน่อย\" หรือ \"ช่วยวาดรูปให้หน่อย\" ดูก่อนได้เลยครับ 😄
          }
        ]);
        continue;
      }

      if (event.type !== "message") continue;

      // ===== กรณีเป็นรูปภาพ (ให้วิเคราะห์ภาพ) =====
      if (event.message.type === "image") {
        try {
          const contentRes = await axios.get(
            `https://api-data.line.me/v2/bot/message/${event.message.id}/content`,
            {
              responseType: "arraybuffer",
              headers: { Authorization: `Bearer ${LINE_TOKEN}` }
            }
          );

          const base64 = Buffer.from(contentRes.data, "binary").toString("base64");
          const analysis = await analyzeImage(base64);
          saveMessage(userId, "assistant", analysis);

          await replyLINE(event.replyToken, [
            {
              type: "text",
              text: analysis,
              quickReply: buildDefaultQuickReply()
            }
          ]);
        } catch (err) {
          console.error("Handle image error:", err.response?.data || err.message);
          await replyLINE(event.replyToken, [
            {
              type: "text",
              text: "ผมอ่านรูปนี้ไม่สำเร็จครับ ลองส่งใหม่อีกครั้งได้ไหมครับ 🙏",
              quickReply: buildDefaultQuickReply()
            }
          ]);
        }
        continue;
      }

      // ===== กรณีเป็นข้อความ =====
      if (event.message.type !== "text") continue;
      const userMessage = (event.message.text || "").trim();
      if (!userMessage) continue;

      // คำสั่ง /reset ล้าง memory ของ user นั้น
      if (userMessage === "/reset") {
        memory[userId] = [];
        // ✅ ล้าง prompt รูปล่าสุดด้วย
        lastImagePrompt[userId] = undefined;
        await replyLINE(event.replyToken, [
          {
            type: "text",
            text: "ผมล้างประวัติการคุยของเราทั้งหมดให้แล้วนะครับ เริ่มคุยใหม่ได้เลย ✨",
            quickReply: buildDefaultQuickReply()
          }
        ]);
        continue;
      }

      saveMessage(userId, "user", userMessage);

      // ✅ เช็กกรณี "วาดต่อ / ต่อ ..." ก่อน (ใช้ prompt เดิม)
      const isContinueImage =
        userMessage === "ต่อ" ||
        userMessage === "วาดต่อ" ||
        userMessage.startsWith("ต่อ ") ||
        userMessage.startsWith("วาดต่อ ");

      if (isContinueImage && lastImagePrompt[userId]) {
        const continuedPrompt =
          buildContinuedPrompt(userId, userMessage) || lastImagePrompt[userId];

        try {
          const imageUrl = await generateImage(continuedPrompt);
          lastImagePrompt[userId] = continuedPrompt;

          await replyLINE(event.replyToken, [
            {
              type: "image",
              originalContentUrl: imageUrl,
              previewImageUrl: imageUrl
            }
          ]);
        } catch (err) {
          await replyLINE(event.replyToken, [
            {
              type: "text",
              text:
                "ผมสร้างรูปต่อไม่สำเร็จครับ ลองใหม่อีกครั้ง หรือเริ่มคำขอวาดรูปใหม่ด้วยคำว่า วาดต่อ... หรือ ต่อ... ครับผม 🎨",
              quickReply: buildDefaultQuickReply()
            }
          ]);
        }
        continue;
      }

      // ตรวจว่าเป็นคำขอวาดรูปไหม (ไม่ต้องพิมพ์ /img)
      const lower = userMessage.toLowerCase();
      const wantImage =
        userMessage.startsWith("วาด") ||
        userMessage.startsWith("สร้างรูป") ||
        userMessage.includes("ช่วยวาด") ||
        userMessage.includes("ขอรูป") ||
        lower.includes("logo") ||
        lower.includes("โลโก้") ||
        lower.includes("โปสเตอร์") ||
        lower.includes("banner");

      if (wantImage) {
        const prompt = userMessage
          .replace(/^วาด\s*/g, "")
          .replace(/^สร้างรูป\s*/g, "")
          .replace("ช่วยวาด", "")
          .replace("ขอรูป", "")
          .trim();

        // ✅ เก็บ prompt ของรูปล่าสุดไว้สำหรับ "วาดต่อ"
        lastImagePrompt[userId] = prompt || userMessage;

        try {
          const imageUrl = await generateImage(prompt);
          await replyLINE(event.replyToken, [
            {
              type: "image",
              originalContentUrl: imageUrl,
              previewImageUrl: imageUrl
            }
          ]);
        } catch (err) {
          await replyLINE(event.replyToken, [
            {
              type: "text",
              text:
                "ผมสร้างรูปไม่สำเร็จครับ อาจมีปัญหาที่ระบบ Stability AI หรือตั้งค่า API key/URL ยังไม่ถูก ลองเช็กแล้วลองใหม่อีกครั้งนะครับ 😢",
              quickReply: buildDefaultQuickReply()
            }
          ]);
        }
        continue;
      }

      // ตรวจว่าควรใช้ Web Search ไหม
      if (await shouldUseWebSearch(userMessage)) {
        const answer = await answerWithWebSearch(userId, userMessage);
        saveMessage(userId, "assistant", answer);
        await replyLINE(event.replyToken, [
          {
            type: "text",
            text: answer,
            quickReply: buildDefaultQuickReply()
          }
        ]);
        continue;
      }

      // ปกติ: ใช้สมองหลักของ Arvin (เลือก OpenAI / Gemini ให้อัตโนมัติ)
      const answer = await arvinBrain(userId, userMessage);
      saveMessage(userId, "assistant", answer);

      await replyLINE(event.replyToken, [
        {
          type: "text",
          text: answer,
          quickReply: buildDefaultQuickReply()
        }
      ]);
    } catch (err) {
      console.error("Event error:", err.response?.data || err.message);
    }
  }
});

// ------------- START SERVER -------------
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`Arvin Super AI with Stability + Gemini is running on port ${PORT}`);
});
